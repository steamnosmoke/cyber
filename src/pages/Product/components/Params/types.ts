export type TCharacter = {
  label: string;
  img: string;
  value: string;
};

export type TTablet = {
  label: string;
  img: string;
  value: string;
};

export type TGallaryProps = {
  images: string[];
};
